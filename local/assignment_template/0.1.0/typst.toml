[package]
name = "assignment_template"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Riu"]
license = "MIT-0"
description = "assignment template"

[template]
path = "template"
entrypoint = "main.typ"
